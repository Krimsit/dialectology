mixin cover(title, description, image)
    .cover
        .container
            .coverContent
                .coverText(class=!image && 'coverTextCentered')
                    h1.coverTextTitle=title
                    if description
                        p.coverTextDescription
                            != description.replace(/\n/g, '<br>')
                if image
                    img(src=basePath + image, altText=title).coverImage